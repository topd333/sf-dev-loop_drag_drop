<%= form_for(@admin_hardware_type) do |f| %>
  <% if @admin_hardware_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@admin_hardware_type.errors.count, "error") %> prohibited this admin_hardware_type from being saved:</h2>

      <ul>
      <% @admin_hardware_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :model_number %><br>
    <%= f.text_field :model_number %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

<%= form_for [@loop_asset, @slide_asset] do |f| %>
  <% if @slide_asset.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@slide_asset.errors.count, "error") %> prohibited this slide_asset from being saved:</h2>

      <ul>
      <% @slide_asset.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :displayname %><br>
    <%= f.text_field :displayname %>
  </div>
  <div class="field">
    <%= f.label :slide_template %><br>
    <%= f.collection_select(:slide_template_id, SlideTemplate.all, :id, :name) %>
  </div>
  <div class="field">
    <%= f.label :duration %><br>
    <%= f.text_field :duration %>
  </div>
  <div class="field">
    <%= f.label :hooks_json_string %><br>
    <%= f.text_field :hooks_json_string %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

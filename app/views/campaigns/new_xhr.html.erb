<%= form_for(@campaign, html: {class: "form-horizontal"}) do |f| %>
  <% if @campaign.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@campaign.errors.count, "error") %> prohibited this loop_asset from being saved:</h2>

      <ul>
      <% @campaign.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <label for="displayname" class="col-sm-3 control-label">Name</label>
    <div class="col-sm-8">
      <%= f.text_field :displayname, class: "form-control" %>
    </div>
  </div>

  <div class="form-group">
    <div class="col-sm-offset-3 col-sm-8">
       <%= f.submit "Create Campaign", class: "btn btn-primary" %>
    </div>
  </div>

<% end %>

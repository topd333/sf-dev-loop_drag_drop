<%= form_for(@media_asset) do |f| %>
  <% if @media_asset.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@media_asset.errors.count, "error") %> prohibited this media_asset from being saved:</h2>

      <ul>
      <% @media_asset.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :asset_file %><br>
    <%= f.file_field :asset_file %>
  </div>
  <div class="field">
    <%= f.label :displayname %><br>
    <%= f.text_field :displayname %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
